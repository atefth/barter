<% if user_signed_in? %>
	<div class="navbar navbar-static-top navbar-inverse">
	    <div class="navbar-inner">
		    <a class="brand" href="/">Barter</a>
		    <ul class="nav pull-right">
			    <li class="<%= 'active' if @page == 'home' %>"><a href="/home/">Home</a></li>
			    <li class="<%= 'active' if @page == 'message' %>"><a href="/messages/">Messages</a></li>
			    <li>
					<div class="btn-group">
						<a href="/users/show/<%= current_user.id %>" class="btn btn-primary <%= 'active' if @page == 'profile' %>"><i class="icon-user icon-white"></i>Profile</a>
						<a href="#" data-toggle="dropdown" class="btn btn-primary dropdown-toggle"><span class="caret"></span></a>
						<ul class="dropdown-menu">
							<li><a href="#"><i class="icon-pencil"></i> Edit</a></li>
							<li><a href="#"><i class="icon-trash"></i> Delete</a></li>
							<li><a href="#"><i class="icon-ban-circle"></i> Ban</a></li>
							<li class="divider"></li>
							<li><a href="#"><i class="i"></i> Make admin</a></li>
						</ul>
					</div>
				</li>
			    <li class="<%= 'active' if @page == 'login' %>">
			    	<% if user_signed_in? %>
			    		<a href="/users/logout">Logout
			    	<% else %>
			    		<a href="/users/login">Login
			    	<% end %>
			    </a></li>
		    </ul>
	    </div>
	</div>
<% end %>